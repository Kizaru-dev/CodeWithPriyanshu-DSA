package ExceptionHandling;

public class Concept_Learn {
    /**
     A) Java Runtime Exception .
     A runtime exception happens  due to a programming error . They are also
     known as unchecked exception .
     These exception are not checked at compile-time but run-time.Some of the
     common runtime exception are :
     -- Null Pointer access (missing the initialization of a variable ) -
     -- Out-of-bounds array access - ArrayIndexOutOfBoundsException
     -- Dividing a number by 0 - ArithmeticException
     (if it's a runtime exception then it's your fault )
     B) Compile Time Exception .
     An IOException is also known as a checked exception. They are checked by the
     compiler at the compile-time and the programmer is prompted to handle these
     exception .
     Some of the examples of checked exception are :
     -- trying to open a file that doesn't exist results in FileNotFoundException
     -- Trying to read past the end of a file .
     */

    public static void main(String[] args) {

    }
}
